import java.io.*;
import java.util.*;
import java.lang.*;
class Calculator
{
static double powerInt(int a,int b)
{
return (Math.pow(a,b));
}
static powerDouble(double a,int b)
{
return Math.pow(a,b);
}
}
class Calculate
{
System.out.println("int"+Calculator.powerInt(12,10));
System.out.println("double"+Calculator.powerDouble(12,10));
